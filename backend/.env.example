# ── Google Gemini ─────────────────────────────────────────────────────────────
# Get your key at https://aistudio.google.com/
GEMINI_API_KEY=your-gemini-api-key-here
GEMINI_CHAT_MODEL=gemini-3.1-flash-lite-preview
GEMINI_EMBEDDING_MODEL=gemini-embedding-001

# ── JWT Auth ──────────────────────────────────────────────────────────────────
# Generate a random secret: python -c "import secrets; print(secrets.token_hex(32))"
JWT_SECRET_KEY=change-this-to-a-random-secret-key-at-least-32-chars
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=60

# ── LangSmith observability (optional) ───────────────────────────────────────
# Get your key at https://smith.langchain.com/
LANGSMITH_API_KEY=
LANGSMITH_PROJECT=Nova
LANGSMITH_TRACING=false

# ── Storage paths (defaults work out of the box) ─────────────────────────────
DATABASE_URL=sqlite+aiosqlite:///./nova.db
CHROMA_PERSIST_DIR=./chroma_data
CHROMA_COLLECTION_NAME=nova_knowledge
PDF_UPLOAD_DIR=./pdf_uploads
KNOWLEDGE_BASE_DIR=./knowledge_base
USER_MEMORY_DIR=./user_memory

# ── RAG config ────────────────────────────────────────────────────────────────
RAG_CHUNK_SIZE=1000
RAG_CHUNK_OVERLAP=200
RAG_TOP_K=5

# ── Ollama (optional — used as fallback before Gemini) ───────────────────────
# Install from https://ollama.com/ and run: ollama pull qwen3.5:9b
# If Ollama is not running, Nova falls back to Gemini automatically.

# JWT Auth
JWT_SECRET_KEY=change-this-to-a-random-secret-key-at-least-32-chars
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=60

# Database (SQLite by default, no extra infra needed)
DATABASE_URL=sqlite+aiosqlite:///./nova.db

# ChromaDB
CHROMA_PERSIST_DIR=./chroma_data
CHROMA_COLLECTION_NAME=nova_knowledge

# PDF uploads
PDF_UPLOAD_DIR=./pdf_uploads

# Knowledge base (auto-ingested on startup)
KNOWLEDGE_BASE_DIR=./knowledge_base

# RAG config
RAG_CHUNK_SIZE=1000
RAG_CHUNK_OVERLAP=200
RAG_TOP_K=5
